{
  "name": "invalid-hook-repro",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/**"
    ]
  },
  "scripts": {
    "build": "yarn workspace components-lib build && yarn workspace app build"
  }
}
